# Public site URL (used for canonical URLs, sitemap, OG metadata)
NEXT_PUBLIC_SITE_URL=https://phibrain.com

# Contact form destination (server-only, never exposed)
CONTACT_INBOX_EMAIL=hello@phibrain.com

# Optional: SMTP / email provider credentials (server-only)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
